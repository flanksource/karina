importConfigs:
  - minimal.yaml
harbor:
  replicas: 1
  bucket: karina-harbor-e2e
  version: v2.0.0
postgresOperator:
  version: v1.3.4.flanksource.1
test:
  exclude:
    - postgres-operator
    - configmap-reloader
    - dex
    - audit
    - encryption
