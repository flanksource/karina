---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: filebeatconfig.filebeat.flanksource.com
spec:
  group: filebeat.flanksource.com
  names:
    kind: FilebeatConfig
    listKind: FilebeatConfigList
    plural: filebeatconfigs
    shortNames:
      - fbconfig
    singular: filebeatconfig
  scope: Namespaced
  validation:
    openAPIV3Schema:
      properties:
        apiVersion:
          description:
            "APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
          type: string
        kind:
          description:
            "Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
          type: string
        metadata:
          type: object
        spec:
          type: object
          properties:
            version:
              type: string
              description:
                "The version of the filebeat container to initialise as a Daemonset"
            output:
              type: string
              description:
                "The output type, can be either 'elasticsearch' or 'logstash'"
              enum:
                - elasticsearch
                - logstash
              default: elasticsearch
            secretref:
              type: string
              description:
                "The name of the secret with the filebeat output details.  Must contain:
                OUTPUT_URL - the address of the output to connect to
                OUTPUT_USERNAME - (Optional, Elasticsearch) The username to connect to the output
                OUTPUT_PASSWORD - (Optional, Elasticsearch) The password to connect to the output"
            index:
              type: string
              description:
                "Optional, the elasticsearch index value"
            prefix:
              type: string
              description:
                "The annotation prefix for filebeat discovery"
          required:
            - version
            - secretref
      type: object
  version: v1
  versions:
    - name: v1
      served: true
      storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
