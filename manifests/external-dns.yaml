# This file was generated by running 'make external-dns.yaml' in manifests directory
apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-dns
  namespace: platform-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: external-dns
rules:
- apiGroups:
  - ""
  resources:
  - endpoints
  - pods
  - services
  verbs:
  - get
  - watch
  - list
- apiGroups:
  - extensions
  - networking.k8s.io
  resources:
  - ingresses
  verbs:
  - get
  - watch
  - list
- apiGroups:
  - ""
  resources:
  - nodes
  verbs:
  - list
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: external-dns-viewer
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: external-dns
subjects:
- kind: ServiceAccount
  name: external-dns
  namespace: platform-system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: external-dns
  namespace: platform-system
spec:
  selector:
    matchLabels:
      app: external-dns
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: external-dns
    spec:
      containers:
      - args:
        {{- range $k, $v := .externalDns.args }}
        - --{{$k}}={{$v}}
        {{- end }}
        image: k8s.gcr.io/external-dns/external-dns:{{.externalDns.version | default "na" }}
        name: external-dns
      serviceAccountName: external-dns
