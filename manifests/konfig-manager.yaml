# This file was generated by running 'make konfig-manager.yaml' in manifests directory
apiVersion: v1
kind: Namespace
metadata:
  labels:
    control-plane: konfig-manager
  name: platform-system
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: konfig-manager
  namespace: platform-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: konfig-manager-leader-election-role
  namespace: platform-system
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  creationTimestamp: null
  name: konfig-manager
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  - secrets
  verbs:
  - '*'
- apiGroups:
  - konfigmanager.flanksource.com
  resources:
  - konfigs
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - konfigmanager.flanksource.com
  resources:
  - konfigs/finalizers
  verbs:
  - update
- apiGroups:
  - konfigmanager.flanksource.com
  resources:
  - konfigs/status
  verbs:
  - get
  - patch
  - update
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: konfig-manager-metrics-reader
rules:
- nonResourceURLs:
  - /metrics
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: konfig-manager-proxy-role
rules:
- apiGroups:
  - authentication.k8s.io
  resources:
  - tokenreviews
  verbs:
  - create
- apiGroups:
  - authorization.k8s.io
  resources:
  - subjectaccessreviews
  verbs:
  - create
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: konfig-manager-leader-election-rolebinding
  namespace: platform-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: konfig-manager-leader-election-role
subjects:
- kind: ServiceAccount
  name: konfig-manager
  namespace: platform-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: konfig-manager
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: konfig-manager
subjects:
- kind: ServiceAccount
  name: konfig-manager
  namespace: platform-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: konfig-manager-proxy-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: konfig-manager-proxy-role
subjects:
- kind: ServiceAccount
  name: konfig-manager
  namespace: platform-system
---
apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: konfig-manager
  name: konfig-manager-metrics-service
  namespace: platform-system
spec:
  ports:
  - name: https
    port: 8443
    targetPort: https
  selector:
    control-plane: konfig-manager
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    control-plane: konfig-manager
  name: konfig-manager
  namespace: platform-system
spec:
  replicas: 1
  selector:
    matchLabels:
      control-plane: konfig-manager
  template:
    metadata:
      labels:
        control-plane: konfig-manager
    spec:
      containers:
      - args:
        - operator
        - --health-probe-bind-address=:8082
        - --metrics-bind-address=127.0.0.1:8081
        - --leader-elect
        - --sync-period={{ .konfigManager.syncPeriod | default "10m" }}
        command:
        - /bin/konfig-manager
        image: docker.io/flanksource/konfig-manager:{{.konfigManager.version | default "na" }}
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8082
          initialDelaySeconds: 15
          periodSeconds: 20
        name: konfig-manager
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8082
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 150m
            memory: 200Mi
          requests:
            cpu: 100m
            memory: 150Mi
        securityContext:
          allowPrivilegeEscalation: false
      - args:
        - --secure-listen-address=0.0.0.0:8443
        - --upstream=http://127.0.0.1:8080/
        - --logtostderr=true
        - --v=10
        image: gcr.io/kubebuilder/kube-rbac-proxy:v0.8.0
        name: kube-rbac-proxy
        ports:
        - containerPort: 8443
          name: https
      securityContext:
        runAsNonRoot: true
      serviceAccountName: konfig-manager
      terminationGracePeriodSeconds: 10